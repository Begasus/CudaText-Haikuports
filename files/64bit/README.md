## Cudatext files

#This is the CudaText executable for Haiku 64bit and the data files that need to be installed to: 
- The directories `files/64bit/data/cudatext/{data,py}` should be installed in `/boot/home/config/non-packaged/data/cudatext`.
- The directory `files/64bit/data/cudatext/settings_default` should be installed to `/boot/home/config/settings/cudatext`.
- The binary `apps/CudaText` installed in `/boot/home/config/non-packaged/bin`.
